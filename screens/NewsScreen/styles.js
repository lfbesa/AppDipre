import {
  Platform,
  StyleSheet,
} from 'react-native';

import {
  BACKGROUND_COLOR,
  FONT_PRIMARY_COLOR,
  INPUT_BACKGROUND_COLOR,
} from '../../styles/common';

export default StyleSheet.create({
	container: {
    flex: 1,
    paddingTop: 15,
    backgroundColor: '#fff',
  },
});
