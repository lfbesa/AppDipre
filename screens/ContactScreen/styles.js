import {
  Platform,
  StyleSheet,
} from 'react-native';

import {
  BACKGROUND_COLOR,
  FONT_PRIMARY_COLOR,
  INPUT_BACKGROUND_COLOR,
} from '../../styles/common';

export default StyleSheet.create({
  wrapper: {
    flex: 1,
    marginTop: 150,
  },
  submitButton: {
    paddingHorizontal: 10,
    paddingTop: 20,
  },
});
